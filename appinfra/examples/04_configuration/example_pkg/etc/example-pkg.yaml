# Packaged base config for example_pkg.
#
# Resolved by ../../library_mode_config_spec.py via ConfigSpec.
# In a real package this file ships inside the wheel; here it lives
# under an examples subtree that plays the same role.

app:
  name: example-pkg
  port: 8080
  greeting: hello from the packaged base config

logging:
  level: info
