# Setuptools build configuration
#
# This file must live in project root (setuptools limitation).
# See: https://github.com/pypa/setuptools/issues/1816
#
# Purpose: Redirect build artifacts to .build/ instead of build/
# to keep hidden files grouped and out of the way.

[build]
build_base = .build
