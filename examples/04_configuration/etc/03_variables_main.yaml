# Variable Substitution with Includes Example
# Variables defined here are used in the included file

pgserver:
  host: localhost
  port: 5432
  user: admin
  pass: secret

dbs: !include './03_variables_databases.yaml'
